# -*- coding: utf-8 -*-
"""Модель команды, получаемой от микроконтроллера.

Содержит класс `Command` для хранения сырого представления команды
в виде списка байтов.
"""

# System imports
from dataclasses import dataclass

# External imports

# User imports

#############################################

@dataclass
class Command:
    """Команда, полученная от микроконтроллера.

    Хранит неинтерпретированные байты команды. Дальнейшая обработка
    (разбор параметров) может выполняться внешними функциями или методами.

    Attributes:
        byte_coding (list[bytes]): Список байтов, составляющих команду.
            Каждый элемент списка — это отдельный байт (объект bytes длины 1).
    """

    byte_coding: list[bytes]